<article>
    {% if post.get_thumbnail %}
        <img src="{{ post.thumbnail.src('medium') }}"
             alt="{{ post.thumbnail.alt }}">
    {% endif %}

    <h2>
        <a href="{{ post.link }}">{{ post.title }}</a>
    </h2>

    <p>{{ post.excerpt.length(144) }}</p>
</article>
