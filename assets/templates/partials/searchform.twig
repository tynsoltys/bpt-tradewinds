<form method="get" role="search" action="{{ site.url }}" class="hidden lg:flex flex-end justify-end items-center">
    <input type="text" placeholder="Search..." name="s" class="rounded pl-4 text-sm tracking-widest">
    <input type="submit" value="Search" class="submit btn ml-2 px-3 text-sm  md:px-4 md:text-md bg-accent-blue">
    <input type="hidden" name="post_type" value="post">
</form>

{# <div x-data="{ searchOpen: false }" class="hidden"></div>
<button @click="{searchOpen = !searchOpen}"><i class="fas fa-search ml-2" ></i></button>
    <form method="get" role="search" action="{{ site.url }}" class="hidden flex-end justify-end items-center" x-bind:class="{ searchOpen == true ? 'bg-accent-green' : 'bg-accent-red' }">
        <div class="bg-brand-white w-full justify-end items-center">
            <input type="text" placeholder="Search..." name="s" class="rounded text-sm tracking-widest">
            <input type="submit" value="Search" class="submit btn ml-2 px-2 py-1 text-sm md:py-2 md:px-4 md:text-md bg-accent-blue mt-1 ">
            <input type="hidden" name="post_type" value="post">
            <i class="fas fa-times ml-2 mt-1"></i>
        </div>

    </form>
</div> #}
