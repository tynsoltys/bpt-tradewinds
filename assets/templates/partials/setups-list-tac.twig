
<div class="" >
    <article class="asset-container grid grid-cols-5 m-2 text-center p-4 rounded rounded-lg" id="equity">
        <div class="col-span-1 flex flex-col flex-wrap rounde justify-center mr-4 py-8 ">
            <h3 class="uppercase text-center font-semibold tracking-widest text-2xl">Long Term Equity</h3>
            <div class="pie-chart rounded-full h-32 w-32  mx-auto my-4">
                <img src="{{theme.link}}/../assets/images/piecharts/pie-equity.png" alt="">
            </div>
            <p class="text-center font-semibold mt-1 text-5xl">25%</p>
            <p class="subheading">target</p>
        </div>

        <div class="col-span-4 flex flex-col justify-center">
            {% for trade in tac %}
            {% if trade.asset_class.slug == ('equity') %}
                {{ include ('partials/setups/setup-card_concise-tac.twig') }}
            {% endif %}
        {% endfor %}
        </div>
    </article>
    <article class="asset-container grid grid-cols-5 m-2 text-center p-4 rounded rounded-lg" id="bonds">
        <div class="col-span-1 flex flex-col flex-wra justify-center mr-4  py-8 ">
            <h3 class="uppercase text-center font-semibold tracking-widest text-2xl">Income & Bonds</h3>
            <div class="pie-chart rounded-full h-32 w-32  mx-auto my-4">
                <img src="{{theme.link}}/../assets/images/piecharts/pie-bonds.png" alt="">
            </div>
            <p class="text-center font-semibold mt-1 text-5xl">20%</p>
            <p class="subheading">target</p>
        </div>

        <div class="col-span-4 flex flex-col justify-center">

            {% for trade in tac %}
            {% if trade.asset_class.slug == ('bonds') %}
                {{ include ('partials/setups/setup-card_concise-tac.twig') }}
            {% endif %}
        {% endfor %}
        </div>
    </article>
    <article class="asset-container grid grid-cols-5 m-2 text-center p-4 rounded rounded-lg" id="metals">
        <div class="col-span-1 flex flex-col flex-wrap justify-center mr-4  py-8 ">
            <h3 class="uppercase text-center font-semibold tracking-widest text-2xl">Precious Metals</h3>
            <div class="pie-chart rounded-full h-32 w-32  mx-auto my-4">
                <img src="{{theme.link}}/../assets/images/piecharts/pie-metals.png" alt="">
            </div>
            <p class="text-center font-semibold mt-1 text-5xl">15%</p>
            <p class="subheading">target</p>
        </div>

        <div class="col-span-4 flex flex-col justify-center">
            {% for trade in tac %}
            {% if trade.asset_class.slug == ('gold') %}
                {{ include ('partials/setups/setup-card_concise-tac.twig') }}
            {% endif %}
        {% endfor %}
        </div>
    </article>
    <article class="asset-container grid grid-cols-5 m-2 text-center p-4 rounded rounded-lg" id="commodities">
        <div class="col-span-1 flex flex-col flex-wrap justify-center mr-4  py-8 ">
            <h3 class="uppercase text-center font-semibold tracking-widest text-2xl">Commodity Trend</h3>
            <div class="pie-chart rounded-full h-32 w-32  mx-auto my-4">
                <img src="{{theme.link}}/../assets/images/piecharts/pie-commodity.png" alt="">
            </div>
            <p class="text-center font-semibold mt-1 text-5xl">15%</p>
            <p class="subheading">target</p>
        </div>

        <div class="col-span-4 flex flex-col justify-center">

            {% for trade in tac %}
            {% if trade.asset_class.slug == ('commodity') %}
                {{ include ('partials/setups/setup-card_concise-tac.twig') }}
            {% endif %}
        {% endfor %}
        </div>
    </article>
    <article class="asset-container grid grid-cols-5 m-2 text-center p-4 rounded rounded-lg" id="volatility">
        <div class="col-span-1 flex flex-col flex-wrap justify-center mr-4  py-8 ">
            <h3 class="uppercase text-center font-semibold tracking-widest text-2xl">Volatility</h3>
            <div class="pie-chart rounded-full h-32 w-32  mx-auto my-4">
                <img src="{{theme.link}}/../assets/images/piecharts/pie-volatility.png" alt="">
            </div>
            <p class="text-center font-semibold mt-1 text-5xl">25%</p>
            <p class="subheading">target</p>
        </div>

        <div class="col-span-4 flex flex-col justify-center">

            {% for trade in tac %}
            {% if trade.asset_class.slug == ('volatility') %}
                {{ include ('partials/setups/setup-card_concise-tac.twig') }}
            {% endif %}
        {% endfor %}
        </div>
    </article>

</div>

