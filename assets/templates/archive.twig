{% extends 'index.twig' %}

{% block body %}
    {{ include ('partials/page-header.twig') }}

    {% for trade in posts %}
        {{ include ('partials/setups/setup-card_concise.twig') }}
    {% endfor %}

    {{ parent() }}
{% endblock body %}
